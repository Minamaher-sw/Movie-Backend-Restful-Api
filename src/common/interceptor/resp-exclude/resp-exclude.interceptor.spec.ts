/* eslint-disable prettier/prettier */
import { RespExcludeInterceptor } from './resp-exclude.interceptor';

describe('RespExcludeInterceptor', () => {
  it('should be defined', () => {
    expect(new RespExcludeInterceptor()).toBeDefined();
  });
});
